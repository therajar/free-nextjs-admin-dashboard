import React from 'react';

// Define interface for NFT item
interface NFTItem {
    // Define your properties here
}

const NFTList: React.FC<{  }> = ({  }) => {
    return (
        <div>
            {/* Render NFT items */}
            {/* You can map over the nfts array and render each item */}
            Test
        </div>
    );
};

export default NFTList;
